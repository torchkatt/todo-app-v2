import React from 'react';

/**
 * Componente ReloadPrompt desactivado visualmente.
 * La comprobación y botón de actualización de la versión PWA se gestionan directamente
 * en la pantalla de Ajustes / Configuración (`SettingsPage.tsx`).
 */
export const ReloadPrompt: React.FC = () => {
  return null;
};

export default ReloadPrompt;
