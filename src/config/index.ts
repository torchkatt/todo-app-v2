/**
 * @file index.ts (src/config)
 * @description Módulo de exportación unificada (Barrel Export) para constantes y tokens de diseño.
 * Permite importar constantes mediante: `import { APP_CONFIG, formatCOP } from '../config';`
 */

export * from './constants';
export * from './theme';
